# 珠免酒鬼人格测试 - 环境变量
# 在 Vercel Dashboard → Settings → Environment Variables 中配置

# PostgreSQL 连接地址（Vercel Postgres 自动生成，无需手动设置）
# POSTGRES_URL=postgresql://...

# JWT 密钥（随机字符串，用于 token 签名）
JWT_SECRET=替换为随机字符串

# 初始化密钥（用于首次创建数据库和管理员账号，设置后可删除此变量）
SETUP_KEY=替换为随机字符串
